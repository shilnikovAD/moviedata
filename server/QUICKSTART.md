# Быстрый старт WebSocket сервера

## Проблема
Если при открытии `ws://localhost:3001` в браузере вы видите ошибку "Cannot GET /", это нормально!

**WebSocket URL (`ws://`) нельзя открыть в браузере!**

## Правильный способ

### 1. Запустите сервер
```bash
cd server
npm install
npm start
```

### 2. Откройте в браузере HTTP URL:
```
http://localhost:3001
```

**Обратите внимание:** `http://` а НЕ `ws://`!

### 3. Что вы увидите
- ✅ Статус сервера
- 📊 Количество активных комнат  
- 🔌 Кнопку для тестирования WebSocket
- 📖 Список API endpoints

### 4. Проверьте работу
Нажмите кнопку "Проверить WebSocket" на главной странице - если видите "✅ Подключение установлено!", значит всё работает!

## Разница между HTTP и WebSocket

- **HTTP** (`http://localhost:3001`) - для браузера, показывает веб-страницу
- **WebSocket** (`ws://localhost:3001`) - для приложения, двусторонняя связь

## Если порт 3001 занят

```bash
# Windows
netstat -ano | findstr :3001
taskkill /F /PID <номер_процесса>

# Linux/Mac
lsof -ti:3001 | xargs kill -9
```

Или используйте другой порт:
```bash
$env:PORT=3002  # Windows PowerShell
npm start
```

```bash
PORT=3002 npm start  # Linux/Mac
```

## Следующий шаг

После запуска сервера настройте клиент:

1. Создайте `.env` в корне проекта:
```env
VITE_WS_URL=ws://localhost:3001
```

2. В `src/services/watchPartyServiceWebSocket.ts` установите:
```typescript
const USE_WEBSOCKET = true;
```

3. В `src/pages/WatchPartyPage.tsx` замените импорт:
```typescript
import { watchPartyService } from '../services/watchPartyServiceWebSocket';
```

4. Запустите клиент:
```bash
npm run dev
```

5. Откройте `http://localhost:5173` и создайте Watch Party!

## Проверка

Откройте в двух разных браузерах (или устройствах):
1. Первый: создайте комнату
2. Второй: присоединитесь по Room ID
3. Управляйте воспроизведением - синхронизация работает! 🎉
