# 🏗️ Архитектура MovieCatalog с Watch Party

## Общая архитектура приложения

```
┌─────────────────────────────────────────────────────────────┐
│                    MovieCatalog App                         │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   React Router                        │  │
│  │                                                       │  │
│  │  /              →  HomePage                          │  │
│  │  /movie/:id     →  MovieDetailsPage                  │  │
│  │  /watch-party/:id → WatchPartyPage 🎉                │  │
│  │  /favorites     →  FavoritesPage                     │  │
│  │  /about         →  AboutPage                         │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Redux Store (State)                      │  │
│  │                                                       │  │
│  │  ┌────────────┐  ┌──────────┐  ┌──────────────┐    │  │
│  │  │  movies    │  │favorites │  │ watchParty 🎉│    │  │
│  │  │  Slice     │  │  Slice   │  │    Slice     │    │  │
│  │  └────────────┘  └──────────┘  └──────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   Services                            │  │
│  │                                                       │  │
│  │  ┌──────────────┐         ┌─────────────────────┐   │  │
│  │  │  movieApi    │         │ watchPartyService 🎉│   │  │
│  │  │   (TMDB)     │         │  (BroadcastChannel) │   │  │
│  │  └──────────────┘         └─────────────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Watch Party Architecture (Детально)

```
┌─────────────────────────────────────────────────────────────┐
│                    WatchPartyPage (UI Layer)                │
│                                                             │
│  ┌─────────────────┐         ┌─────────────────────────┐  │
│  │  Video Player   │         │      Sidebar            │  │
│  │  ┌───────────┐  │         │  ┌─────────────────┐   │  │
│  │  │ Placeholder│  │         │  │  Participants   │   │  │
│  │  │  (16:9)   │  │         │  │  • John (Host)  │   │  │
│  │  └───────────┘  │         │  │  • Jane         │   │  │
│  │                 │         │  └─────────────────┘   │  │
│  │  ┌───────────┐  │         │                        │  │
│  │  │ Controls  │  │         │  ┌─────────────────┐   │  │
│  │  │ ▶️ ⏸️ ⏱️  │  │         │  │      Chat       │   │  │
│  │  └───────────┘  │         │  │  [Messages...]  │   │  │
│  └─────────────────┘         │  │  [Input box]    │   │  │
│                               │  └─────────────────┘   │  │
│                               └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
                    (dispatch/useSelector)
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│              Redux Store - watchPartySlice                  │
│                                                             │
│  State: {                                                   │
│    roomId: string | null                                    │
│    isHost: boolean                                          │
│    participants: Participant[]                              │
│    currentTime: number                                      │
│    isPlaying: boolean                                       │
│    movieId: number | null                                   │
│    connected: boolean                                       │
│    messages: ChatMessage[]                                  │
│  }                                                          │
│                                                             │
│  Actions:                                                   │
│    • createRoom()                                           │
│    • joinRoom()                                             │
│    • leaveRoom()                                            │
│    • addParticipant()                                       │
│    • setPlaying()                                           │
│    • addMessage()                                           │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
                    (service calls)
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│              watchPartyService (Business Logic)             │
│                                                             │
│  Methods:                                                   │
│    • connect(roomId, userId, userName)                      │
│    • disconnect()                                           │
│    • play(roomId, userId, time)                             │
│    • pause(roomId, userId, time)                            │
│    • seek(roomId, userId, time)                             │
│    • sendChat(roomId, userId, userName, message)            │
│    • on(event, callback)                                    │
│    • off(event, callback)                                   │
│                                                             │
│  Events:                                                    │
│    • 'join'    - User joined                                │
│    • 'leave'   - User left                                  │
│    • 'play'    - Playback started                           │
│    • 'pause'   - Playback paused                            │
│    • 'seek'    - Time changed                               │
│    • 'chat'    - New message                                │
└─────────────────────────────────────────────────────────────┘
                           ↓ ↑
                    (postMessage)
                           ↓ ↑
┌─────────────────────────────────────────────────────────────┐
│           BroadcastChannel API (Communication)              │
│                                                             │
│  Channel: watchparty_{roomId}                               │
│                                                             │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐         │
│  │  Tab 1   │ ←──→ │ Channel  │ ←──→ │  Tab 2   │         │
│  │  User1   │      │          │      │  User2   │         │
│  └──────────┘      └──────────┘      └──────────┘         │
│                                                             │
│  Messages: { type, roomId, userId, userName, data }        │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow (Поток данных)

### Создание комнаты

```
User clicks "Create Watch Party"
         ↓
WatchPartyPage: handleCreateRoom()
         ↓
WatchPartyService.generateRoomId()
         ↓
watchPartyService.connect(roomId, userId, userName)
         ↓
BroadcastChannel created & joined
         ↓
dispatch(createRoom({ roomId, movieId, userId, userName }))
         ↓
Redux State Updated
         ↓
UI Re-renders with room info
```

### Синхронизация Play/Pause

```
User clicks Play button (Tab 1)
         ↓
handlePlayPause() called
         ↓
watchPartyService.play(roomId, userId, currentTime)
         ↓
BroadcastChannel.postMessage({ type: 'play', ... })
         ↓
Other tabs receive message via onmessage
         ↓
watchPartyService.emit('play', data)
         ↓
Event listener in useEffect
         ↓
dispatch(setPlaying(true))
         ↓
Redux State Updated in all tabs
         ↓
UI Re-renders → Video plays in Tab 2
```

### Отправка сообщения в чат

```
User types message and clicks Send
         ↓
handleSendMessage()
         ↓
watchPartyService.sendChat(roomId, userId, userName, message)
         ↓
BroadcastChannel.postMessage({ type: 'chat', ... })
         ↓
dispatch(addMessage({ id, userId, userName, message, timestamp }))
         ↓
Message added to local state
         ↓
Other tabs receive via BroadcastChannel
         ↓
dispatch(addMessage(...)) in other tabs
         ↓
Chat displays message in all tabs
```

## Component Hierarchy

```
App
└── BrowserRouter
    └── Header
    └── Routes
        ├── HomePage
        │   ├── SearchBar
        │   └── MovieCard (multiple)
        │
        ├── MovieDetailsPage
        │   └── [Watch Party Button] 🎉
        │
        ├── WatchPartyPage 🎉
        │   ├── Setup Screen (conditional)
        │   │   ├── Name Input
        │   │   ├── Create Room Button
        │   │   └── Join Room Input
        │   │
        │   └── Party Screen (conditional)
        │       ├── Video Player
        │       │   ├── Placeholder
        │       │   └── Controls
        │       │
        │       └── Sidebar
        │           ├── Participants List
        │           └── Chat
        │               ├── Messages
        │               └── Input
        │
        ├── FavoritesPage
        │   └── MovieCard (multiple)
        │
        └── AboutPage
```

## State Management Flow

```
┌──────────────────────────────────────────────────────┐
│              Application State (Redux)               │
├──────────────────────────────────────────────────────┤
│                                                      │
│  movies: {                                           │
│    movies: Movie[]           ← TMDB API             │
│    movieDetails: MovieDetails                        │
│    searchResults: Movie[]                            │
│    loading: boolean                                  │
│    error: string | null                              │
│  }                                                   │
│                                                      │
│  favorites: {                                        │
│    favorites: Movie[]        ← localStorage         │
│  }                                                   │
│                                                      │
│  watchParty: { 🎉                                    │
│    roomId: string | null                             │
│    isHost: boolean                                   │
│    participants: Participant[]                       │
│    currentTime: number       ← Synchronized         │
│    isPlaying: boolean        ← Synchronized         │
│    movieId: number | null                            │
│    connected: boolean                                │
│    messages: ChatMessage[]   ← BroadcastChannel     │
│  }                                                   │
└──────────────────────────────────────────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│         Frontend Framework              │
│              React 19                   │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         Type Safety                     │
│           TypeScript                    │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│      State Management                   │
│        Redux Toolkit                    │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│          Routing                        │
│       React Router v7                   │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         Styling                         │
│        CSS Modules                      │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│      Build Tool                         │
│          Vite 7                         │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│        Testing                          │
│  Vitest + Testing Library + Cypress     │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│    Component Documentation              │
│         Storybook                       │
└─────────────────────────────────────────┘
```

## Watch Party Communication (Current vs Production)

### Current Implementation (Demo)

```
┌──────────┐                    ┌──────────┐
│  Tab 1   │                    │  Tab 2   │
│  (User1) │                    │  (User2) │
└────┬─────┘                    └────┬─────┘
     │                                │
     │      BroadcastChannel         │
     ├────────────────────────────────┤
     │     (Same Computer Only)       │
     │                                │
     └────────────────────────────────┘
```

### Production Implementation (Recommended)

```
┌──────────┐      ┌──────────────┐      ┌──────────┐
│ Device 1 │      │   WebSocket  │      │ Device 2 │
│ (User1)  │◄────►│    Server    │◄────►│ (User2)  │
└──────────┘      │  (Socket.io) │      └──────────┘
                  └──────┬───────┘
                         │
                  ┌──────▼───────┐
                  │   Database   │
                  │  (Optional)  │
                  └──────────────┘
```

---

## 📚 Полезные ссылки

- **React**: https://react.dev/
- **Redux Toolkit**: https://redux-toolkit.js.org/
- **React Router**: https://reactrouter.com/
- **BroadcastChannel API**: https://developer.mozilla.org/en-US/docs/Web/API/Broadcast_Channel_API
- **Socket.io**: https://socket.io/
- **TMDB API**: https://www.themoviedb.org/documentation/api

---

**Архитектура спроектирована для масштабируемости и легкой миграции на WebSocket!** 🚀

