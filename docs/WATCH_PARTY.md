# 🎉 Watch Party Feature

## Описание

Watch Party - это функция совместного просмотра фильмов в реальном времени. Пользователи могут создавать "комнаты" для просмотра фильмов вместе с друзьями, с синхронизированным воспроизведением и чатом.

## Возможности

- ✨ **Создание комнаты**: Создайте уникальную комнату для просмотра
- 🔗 **Присоединение по ссылке**: Поделитесь ссылкой с друзьями
- ▶️ **Синхронизация воспроизведения**: Play/Pause синхронизируется между всеми участниками
- 💬 **Чат в реальном времени**: Общайтесь во время просмотра
- 👥 **Список участников**: Видьте, кто смотрит вместе с вами
- 👑 **Хост комнаты**: Создатель комнаты становится хостом

## Как использовать

### 1. Создание Watch Party

1. Перейдите на страницу любого фильма
2. Нажмите кнопку "🎉 Watch Party"
3. Введите ваше имя
4. Нажмите "Create New Watch Party"
5. Скопируйте ссылку на комнату и отправьте друзьям

### 2. Присоединение к Watch Party

1. Получите ссылку от друга или введите Room ID вручную
2. Нажмите на ссылку или введите Room ID
3. Введите ваше имя
4. Нажмите "Join Watch Party"

### 3. Управление воспроизведением

- Нажмите "▶️ Play" для начала воспроизведения
- Нажмите "⏸️ Pause" для паузы
- Все действия синхронизируются между участниками

### 4. Использование чата

- Введите сообщение в поле чата
- Нажмите "Send" или клавишу Enter
- Ваше сообщение увидят все участники

## Техническая реализация

### Архитектура

```
WatchPartyPage (UI)
     ↓
watchPartySlice (Redux State)
     ↓
watchPartyService (Communication)
     ↓
BroadcastChannel API (Sync)
```

### Используемые технологии

- **Redux Toolkit**: Управление состоянием Watch Party
- **BroadcastChannel API**: Синхронизация между вкладками браузера
- **localStorage**: Персистентность данных комнаты

### Структура состояния

```typescript
interface WatchPartyState {
  roomId: string | null;
  isHost: boolean;
  participants: Participant[];
  currentTime: number;
  isPlaying: boolean;
  movieId: number | null;
  connected: boolean;
  messages: ChatMessage[];
}
```

## Ограничения текущей реализации

⚠️ **Важно**: Текущая реализация использует `BroadcastChannel API` для демонстрации, что работает только между вкладками на **одном компьютере**.

### Для production-использования:

Для работы Watch Party между разными устройствами необходимо:

1. **Настроить WebSocket сервер**:
   ```javascript
   // Пример с Socket.io
   import io from 'socket.io-client';
   const socket = io('wss://your-server.com');
   ```

2. **Заменить BroadcastChannel на WebSocket** в `watchPartyService.ts`

3. **Добавить аутентификацию** для безопасности комнат

4. **Реализовать настоящий video player** (например, используя Video.js или Plyr)

## Примеры интеграции с WebSocket

### Backend (Node.js + Socket.io)

```javascript
const io = require('socket.io')(3001, {
  cors: { origin: '*' }
});

io.on('connection', (socket) => {
  socket.on('join-room', ({ roomId, userId, userName }) => {
    socket.join(roomId);
    socket.to(roomId).emit('user-joined', { userId, userName });
  });

  socket.on('play', ({ roomId, currentTime }) => {
    socket.to(roomId).emit('play', { currentTime });
  });

  socket.on('pause', ({ roomId, currentTime }) => {
    socket.to(roomId).emit('pause', { currentTime });
  });

  socket.on('chat-message', ({ roomId, message, userName }) => {
    socket.to(roomId).emit('chat-message', { message, userName });
  });
});
```

### Frontend (модификация watchPartyService.ts)

```typescript
import io from 'socket.io-client';

class WatchPartyService {
  private socket: Socket;

  connect(roomId: string, userId: string, userName: string) {
    this.socket = io('wss://your-server.com');
    
    this.socket.emit('join-room', { roomId, userId, userName });
    
    this.socket.on('play', (data) => {
      this.emit('play', data);
    });
    
    // ... остальные обработчики
  }
}
```

## Roadmap

- [ ] Интеграция с WebSocket сервером
- [ ] Настоящий video player с поддержкой разных форматов
- [ ] Реакции и эмодзи в чате
- [ ] Голосовой чат
- [ ] Управление правами доступа (только хост может управлять)
- [ ] История просмотров
- [ ] Планирование watch parties

## Поддержка браузеров

| Браузер | BroadcastChannel | WebSocket |
|---------|------------------|-----------|
| Chrome  | ✅ 54+           | ✅ 43+    |
| Firefox | ✅ 38+           | ✅ 48+    |
| Safari  | ✅ 15.4+         | ✅ 10+    |
| Edge    | ✅ 79+           | ✅ 14+    |

## FAQ

**Q: Почему я не вижу других участников?**
A: Убедитесь, что вы используете один и тот же Room ID. В текущей версии оба участника должны быть на одном компьютере (разные вкладки браузера).

**Q: Можно ли использовать это с реальными видео?**
A: Текущая версия - это демо. Для работы с реальными видео нужно интегрировать video player и получить права на контент.

**Q: Как защитить комнату паролем?**
A: Добавьте поле `password` в создание комнаты и проверяйте его при присоединении.

## Контрибьюция

Хотите улучшить Watch Party? Вот как вы можете помочь:

1. Форкните репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit ваши изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

Этот проект использует MIT лицензию.

